/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: [], // Jika ada domain gambar eksternal, tambahkan di sini
    },
};

module.exports = nextConfig;  